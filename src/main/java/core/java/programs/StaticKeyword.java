package core.java.programs;

public class StaticKeyword {
   /* public static void main(String[] args) {
        // Static variables are shared across all instances of the class
        // Static methods can be called without creating an instance of the class
        // Static blocks are executed when the class is loaded into memory
        // Static classes are nested classes that can be accessed without an instance of the outer class

        System.out.println("Static variable: " + StaticClass.a);
    }
}
public static class StaticClass {
    static int a = 10;
    static int b = 20;

    public static accessParameters() {
        System.out.println("a: " + a);
        System.out.println("b: " + b);
        System.out.println("Sum: " + (a + b));
    }*/
}